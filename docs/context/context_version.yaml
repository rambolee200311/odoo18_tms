# ==============================================
# 上下文资产版本管理中心
# 适配：Odoo18 TMS 物流项目
# ==============================================

context_version: 1.0.64
update_time: 2026-08-05
adapt_sprint: Sprint49
update_author: AI-Engineering-Team

version_log:
  1.0.0: {update_content: 初始化四层上下文资产 + 意图契约体系落地, update_type: 全量新建, effect_scope: 全局}
  1.0.1: {update_content: Sprint1 pickup.plan 提货需求模型与视图, update_type: 迭代更新, effect_scope: 提货需求基座}
  1.0.2: {update_content: 新增认知控制工程资产 + Sprint2 排期日历契约, update_type: 资产新增, effect_scope: 双控体系激活}
  1.0.3: {update_content: Sprint2 schedule.plan.schedule 排期模型开发, update_type: 迭代开发, effect_scope: 排期调度模块}
  1.0.4:
    update_content: |
      【架构重校正】transport.request 修正为全流程统一入口
      pickup.plan 降级为计划驱动型排期子单据
      双流程链路：
        计划驱动: transport.request → Schedule → pickup.plan → transport.order
        商务报价: transport.request → inquiry → quote → transport.order
      同步修正全部认知资产（business/ architecture/ history/ design/ requirement）
      新建 Sprint3 意图契约 INT-TMS-SPRINT3-001
    update_type: 架构校正 + 版本基线切换
    effect_scope: 全局流程入口 + 商务报价链路启动

  1.0.5:
    update_content: |
      Sprint4 完成：transport.order 统一收敛闭环
      双链路落单：pickup.plan → order（计划驱动）/ quote → order（商务报价）
      新增 source_type 溯源体系、pickup_plan_id 上游绑定、状态联动回写
      全系统三轮闭环：Sprint1 pickup.plan → Sprint2 schedule → Sprint3 request 入口 → Sprint4 order 出口
    update_type: 核心业务闭环开发
    effect_scope: 运输执行层底座

  
  1.0.6:
    update_content: |
      Sprint5 完成：全局费用/费率底层底座
      新建三层费用模型：FeeType（费用类型字典）+ RateBase（费率底座）+ FeeLine（费用明细行）
      quote / order 通过 _inherit 增量挂载 fee_line_ids
      双链路费用隔离：source_type = commercial / plan_driven
      纯增量底座，零侵入 S1-S4 存量代码
    update_type: 底层基础底座开发
    effect_scope: 费用费率标准底座层
valid_rule:
  - 每次迭代必须同步更新本版本文件
  - 禁止人工擅自删除/修改 forbidden_change.yaml 刚性约束
  - 意图契约变更必须升级小版本号
  - 架构/业务规则变更必须升级次版本号

1.0.7:
  update_content: |
    Sprint6 完成：费用模型重构 — fee_type 指向全局 charge.item + 双向计费
    • transport.fee.type 移除，fee.line.fee_type_id → world.depot.charge.item（全局费用主数据）
    • transport.rate.base.fee_type_id 同步指向 world.depot.charge.item
    • transport.fee.line 新增 party_type（customer_charge / carrier_cost）双向计费隔离
    • transport.fee.line 新增 partner_id 对手方字段
    • __manifest__.py depends 追加 worlddepot（基础主数据模块）
    • forbidden_change.yaml 追加 soft_reference_allow 例外
  update_type: 架构修正 + 功能完善
  effect_scope: 全局费用主数据引用策略 + 双向计费模型

  1.0.10:
    update_content: |
      BUG-010 修复: BUG-009修复代码未落地—customs_declaration_ref字段实际补入transport_order.py
      治理流程升级: bug_fix_workflow.yaml v2.1—新增step_4_5修复代码落地验证+step_6 pre_check
      工程能力升级: odoo_check.py venv路径修正，避免pytz模块未找到
    update_type: Bug修复 + 治理流程升级
    effect_scope: transport.order模型字段 + Bug修复工作流

  1.0.11:
    update_content: |
      BUG-011 修复: tlmp_menus.xml 父菜单后置引用—menu_tlmp_config移至子菜单前
      BUG-012 修复: schedule_calendar_views.xml view_mode"tree"→"list"
      工程能力: verify.py check_7误报63→0修复 + check_8 menuitem顺序校验 + check_5 view_mode扩展
    update_type: Bug修复 + 工程能力升级
    effect_scope: verify.py 8门禁体系

  1.0.12:
    update_content: |
      运行时集成测试底座:
      - tests/test_runtime_validation.py (4项验证: view_mode/menuitem/action_view_ref)
      治理资产升级:
      - pipeline_check.yaml: check_8(menuitem顺序) + check_9(运行时测试)
      - bug_fix_workflow.yaml: step_5新增 post_check 集成测试要求
      工程能力: 静态门禁(8项) + 运行时门禁(1项) 并行双检体系
    update_type: 工程能力升级
    effect_scope: 全模块测试覆盖 + 治理规则升级

  1.0.13:
    update_content: |
      Sprint10 核心业务单元测试:
      - 4个测试文件: test_transport_request / test_pickup_plan / test_inquiry_quote / test_transport_order
      - 34项 TestCase, 34/39 通过(5项因模型层partner_id required/reference验证阻塞)
      覆盖: request_type分流/state/destination约束/pickup无状态机/10阶order状态机/双来源/six态inquiry/six态quote
    update_type: 业务单元测试覆盖
    effect_scope: 双链路核心模型测试覆盖


  1.0.16:
    update_content: |
      Sprint13 Context Runtime v3 — context_loader.py 从目录扫描器升级为前置开发门禁:
      7项能力: encoding/YAML回退/基线校验/风险阻断/退出码/JSON输出/资产摘要
    update_type: 工程基础设施重构
    effect_scope: context_loader.py + 开发前置流程
  1.0.17:
    update_content: |
      CMR 上下文资产更新：双路径设计决策入库
      - dependency.yaml: tlmp.cmr 模块依赖定义完成
      - module_map.md: CMR 数据模型字段定义完成
      - 修复 dependency.yaml 中的孤儿行 creates: ["tlmp.transport.order"]
    update_type: 上下文资产更新
    effect_scope: CMR 模块 + 上下文资产维护


  1.0.19:
    update_content: |
      Sprint14 CMR 运单制作与打印：完整功能开发
      - 新增 tlmp.cmr.line 货物明细子模型
      - 新增 tlmp.cmr.coordinate XY 坐标配置模型
      - 增强 tlmp.cmr: line_ids/累加校验/快速创建/套打辅助方法
      - 重写 cmr_views.xml 完整表单（6 tab）
      - 重写 report_cmr.xml 210×310mm 纯文本套打 PDF
      - 菜单调整: CMR 迁移至 Transport Execution, 坐标配置放 Configuration
      - verify.py 8/8 PASS, odoo_check.py DB 不可用
    update_type: 新功能开发
    effect_scope: CMR 模块 + 坐标配置 + 报表套打


  1.0.21:
    update_content: |
      Sprint15 CMR 单元测试覆盖完成
      - 新增 test_cmr.py 含 30 个 TestCase
      - CMR CRUD/状态机/Line增删改自动计算/累加校验/快速创建/辅助方法
      - ADR关联/唯一约束/坐标配置CRUD
      - verify 8/8 + odoo_check PASS
      - test_runner: 30/30 CMR case PASS, 1 pre-existing pickup_plan fail
    update_type: 单元测试覆盖
    effect_scope: tlmp.cmr + tlmp.cmr.line + tlmp.cmr.coordinate


  1.0.22:
    update_content: |
      Intent 管理体系升级至 5.x（基建改造类）
      - intent_contract.template.yaml 升级至 v2.0: 添加 intent_type/sub_type 分类
      - 新增 bind_asset_list（按 Domain 懒加载）/ deliverables / risk_precheck
      - cognition_asset_map.md: 新增 5.x 分类模型章节
      - cognition_rule.yaml: step 8 扩展为分类加载
      - cognition_refresh.yaml: 新增按 intent_type 沉淀规则
      - cognition_consistency_check.yaml: 新增 intent 类型一致性校验（5项）
    update_type: 基建改造
    effect_scope: intent 模板 + cognition 4 资产 + context_loader


  1.0.23:
    update_content: |
      Intent 管理体系 5.x 最终规范全量落地（基建改造类）
      核心改动：
      1. intent_contract.template.yaml v3.0 全量重写
         - work_type（4类）+ change_type（10枚举）双层分类
         - 移除 bind_asset_list, 改为 asset_snapshot_profile
         - 新增 loop_policy（含 retry_scope affected_asset_only）
         - 新增 affected_assets / validation_strategy / source
         - 移除旧 input/output/workflow 块
         - limit_constraints 替代 limit_points
      2. 新建 profiles/ 目录 + 4 个 Profile yaml（含 include/exclude/load_strategy）
      3. 新建 intent_records/ 目录（执行记录归档）
      4. 4 个认知资产全面对齐新术语
      5. 状态机简化为: CREATED→PREPARED→EXECUTING→VALIDATING→REWORK→ACCEPTED→ARCHIVED
         异常态: BLOCKED / REJECTED / CANCELLED
    update_type: 基建改造
    effect_scope: intent 模板 + profiles/ + cognition 4资产 + intent_records/n

  1.0.24:
    update_content: |
      Sprint16 运输订单跟踪核心基座 — 意图契约定义
      - transport_order 增量扩展（场景枚举/柜号集合/三层地址/tracking_state）
      - Transport Event 动态运输事件（8 类型 + 3 层时间 + 时序约束）
      - Transport Exception 异常闭环（4 态 + 12 类型）
      - Extra Charge 途中额外费用台账（9 费用类型）
      - MRN/T1/ADR 留待 Sprint17 分批落地
    update_type: 新功能开发
    effect_scope: transport_order 扩展 + transport_tracking 新模块（event/exception/charge）


  1.0.27:
    update_content: |
      Sprint17 场景/事件类型/场景路径可配置化管理 + scene_id 贯穿
      - 新增 tlmp.transport.scene 场景档案（8 预设 + CRUD）
      - 新增 tlmp.transport.event.type 事件类型档案（8 预设 + CRUD）
      - 新增 tlmp.transport.scene.event 场景-事件路径映射
      - transport_order: transport_scene Selection → scene_id Many2one
      - transport_event: event_type Selection → event_type_id Many2one
      - BASE_EVENT_ORDER 硬编码 → 基于 scene.event 路径配置驱动时序约束
      - transport_request 新增 scene_id（request→order 全链路贯穿）
      - transport_quote _auto_create_order 同步拷贝 scene_id
    update_type: 新功能开发 + 架构改造
    effect_scope: transport_scene + transport_event_type + scene_event + field migration


  1.0.31:
    update_content: |
      Sprint21 架构冻结验证 — 全量测试覆盖
      - 6 个测试文件，44 个 TestCase
      - test_transport_scene: scene/event/scene_event/cargo_rule 覆盖
      - test_transport_event: event 状态机/时序/exception/charge 覆盖
      - test_transport_cargo: cargo line/cmr sync/snapshot 覆盖
      - test_product_adr: product ADR/MRN/T1 覆盖
      - test_transport_security: 权限隔离覆盖
      - test_transport_flow: 端到端链路 + 附件聚合覆盖
      - verify 8/8 PASS, odoo_check PASS
    update_type: 测试覆盖
    effect_scope: Sprint16-20 全模块测试加固


  1.0.32:
    update_content: |
      Sprint22 运输运营监控 + 时效异常报表 + Cargo Rule 配置化
      - Dashboard Service AbstractModel + TransientModel 监控视图
      - 3 类超时卡片：事件超时/T1 超期/异常超时
      - 时效报表（scene/carrier/month）+ 异常报表（type/scene/carrier）
      - Cargo Rule 配置化 + priority + condition_domain（预留）
      - timeout_hours 字段按异常类型预设默认值
      - t1_deadline 字段 + T1 超期 Dashboard 监控
      - verify 8/8 PASS, odoo_check PASS
    update_type: 新功能开发
    effect_scope: Dashboard + Report + Cargo Rule config


  1.0.33:
    update_content: |
      DGD 需求分析 + 知识资产更新（Sprint23 前序调研）
      - 阅读 DGD 需求说明书 + 实拍表单图片
      - stock_rule.md: §8 DGD 危险品申报单规则
      - inventory_flow.md: §3.8 DGD 申报流程
      - module_map.md: 新增合规文件层（transport.un_dictionary/dgd/dgd.line/dgd_void_log）
      - core insight: DGD cargo line 复用 cargo_line 数据源（非新建模型）
    update_type: 知识沉淀
    effect_scope: business + architecture 资产更新



  1.0.34:
    update_content: |
      Sprint23 DGD 危险品申报单 — ADR 合规文件基座
      - 新增 tlmp.transport.un.dictionary UN 字典模型
      - 新增 tlmp.transport.dangerous.goods.profile DG Profile 模型
      - 新增 tlmp.transport.dgd DGD 主表（六状态生命周期+cmr_id预留字段）
      - 新增 tlmp.transport.dgd.line DGD 快照副本（source_cargo_line_id+ADR法规字段）
      - 新增 tlmp.transport.dgd.void.log 作废审计日志模型
      - cargo_line 增量接入 dangerous_goods_profile_id
      - transport_order 增量接入 dgd_ids + 修复 t1_deadline 重复字段缺陷
      - UN 字典预设数据（12 条高频危险品）+ CRUD 维护页面
      - DGD 表单/列表/搜索视图 + 运输订单 DGD notebook page
      - Compliance Officer 角色 + 权限隔离
      - 20 个自动化测试用例（UN/DGD/生命周期/校验/防重复/快照隔离/作废重生成）
    update_type: 新功能开发
    effect_scope: compliance domain - DGD 模块


  1.0.35:
    update_content: |
      Bug Fix: transport_event @depends('event_type') → event_type_id
      Sprint16 字段改名残留：@depends/方法名/@constrains 共 3 处 event_type 引用未同步迁移
      - 修复后级联解决 ir.model.access.csv model_tlmp_transport_tracking 无法解析问题
    update_type: Bug修复
    effect_scope: transport_tracking 模块加载修复

bind_asset_folder:
  - architecture
  - business
  - history
  - constraints
  - intent
  - cognition

  1.0.42:
    update_content: |
      Sprint27 承运商结算数据底座 — Billing Document + Charge Type + Allocation
      - 新增 5 个模型：charge.type / billing.document / billing.line / allocation / allocation.history
      - 新增 settlement_clerk 角色（继承 operator，禁止删除）
      - carrier_settlement 存量模型增加 billing_document_id 关联
      - transport_order 增加 allocation_ids 关联 + allocated_carrier_cost 计算字段
      - charge.type 预设 11 种高频费用类型（6 大类）
      - Billing Document 简化状态机 Draft->Confirmed->Cancelled
      - Allocation 金额约束（sum <= line_total, 非负, 唯一）
      - 26 个自动化测试用例
      - verify 8/8 PASS
    update_type: 新功能开发
    effect_scope: 承运商结算数据底座（Settlement Domain）

  1.0.43:
    update_content: |
      Sprint28 物流业务索引层 — transport.reference
      - 新增 tlmp.transport.reference 模型（ref_type/ref_value/reference_role/source_system）
      - 核心关联使用 res_model + res_id（Char+Integer），不绑定固定 Many2one
      - Odoo Reference 保留仅作为弱关联（UI 跨模型跳转）
      - 同一 ref_value 允许多条记录（container_no 等设备编号跨运输合法）
      - reference_role 分层：identifier/equipment/document/external
      - transport.order 创建时自动生成 reference + action_open_references 方法
      - 10 个自动化测试用例
      - verify 8/8 PASS, odoo_check PASS
    update_type: 新功能开发
    effect_scope: 物流业务引用索引层

  1.0.44:
    update_content: |
      Sprint29 承运商匹配建议基石 — match.suggestion
      - 新增 tlmp.carrier.match.rule（match_ref_type+match_ref_value 结构化字段）
      - 新增 tlmp.carrier.match.suggestion（candidate_reference/confidence_score/confidence_source/state）
      - 新增 tlmp.carrier.matching.history（from_state/to_state 审计）
      - 新增 TransportMatchService（AbstractModel 匹配建议服务）
      - 新增 AllocationService（services/ 独立文件）
      - transport.reference 增量：valid_from/valid_to + reference_scope + unique 约束
      - billing.line 增量：suggestion_ids 集成
      - 新增 Settlement Config Manager 角色
      - 14 个自动化测试用例（rule/suggestion lifecycle/service）
      - verify 8/8 PASS, odoo_check PASS
    update_type: 新功能开发
    effect_scope: 承运商匹配规则域

  1.0.51:
    update_content: |
      Sprint35 bug fix: Odoo 18 RNG 校验问题修复
      - 7 个 XML 文件移除 <data> 包装，noupdate 移至 <odoo>
      - data/sequences.xml 修复 4 个嵌套 <record> 标签结构
      - 只修改模块文件，不动 Odoo 原生代码
      - 写入教训：RNG 校验陷阱、XML 结构规范、sed 替换风险、沙箱局限
    update_type: Bug修复
    effect_scope: 全模块 XML 数据文件合规
  1.0.52:
    update_content: |
      Sprint36-A Carrier Invoice Import — Settlement External Intake Layer
      - 新增 Settlement Intake Layer 作为唯一外部数据入口
      - Invoice Template: field transformation rules + target_field whitelist
      - Import Batch + Import Line: 完整 Intake 生命周期 + waiting_confirm + partial_failure
      - 服务层: Parser/Validator/Writer 三服务 + SHA256 hash 幂等
      - Billing: external_invoice_no/invoice_version/idempotency_hash/import_batch_id/import_line_id
      - 版本策略 supersede（不覆盖） + line-isolated processing
      - 6 个测试文件覆盖 parser/validator/flow/partial_failure/supersede/security_boundary
      - docs/context/invoice_mapping_schema.yaml 上下文资产
    update_type: 新功能开发 + 领域边界建立
    effect_scope: settlement_intake_layer
  1.0.53:
    update_content: |
      Sprint36-B Settlement Domain Invariant 自动化质量门禁
      - test_settlement_regression.py（单入口文件，4 个内部 class）
      - 覆盖金额守恒/状态机/幂等/权限四大 invariant
      - Domain Invariant 纳入 Governance Asset
      - 测试失败只记录缺陷，不自动修复业务逻辑
    update_type: 质量硬化
    effect_scope: settlement_domain_invariant
  1.0.54:
    update_content: |
      Sprint37 Settlement Exception Domain v1.0
      - 新增 tlmp.settlement.exception（source_reference + snapshot 运营证据链）
      - 状态机 new→assigned→processing→resolved→closed|cancelled
      - Auto Resolution 白名单（仅 DUPLICATE_INVOICE）
      - Exception Handler Registry（预留 Sprint38 Rule Engine 接管）
      - Review Queue 视图（优先级待处理队列）
      - SLA 策略资产化（governance/sla_policy.yaml）
      - 6 个 Domain Invariant（closed_immutable/assigned_requires_owner/...）
    update_type: 新功能开发 + 领域边界建立
    effect_scope: settlement_exception_domain

  1.0.55:
    update_content: |
      Sprint38 Settlement Exception Rule Engine
      - Rule Model: condition_expression + field_catalog + scope + version + lifecycle
      - Rule Engine: Loader/Evaluator/ActionExecutor (idempotent + duplicate prevention)
      - Rule Execution: only MATCHED hits, execution_id unique idempotency
      - Governance: 4 assets (rule_policy/dsl/action_policy/field_catalog)
      - Legacy Handler kept as fallback (Sprint40 removal)
      - Invariants: single_active_rule_version, duplicate_exception_prevention, condition_field_whitelist, action_execution_idempotent
      - 8 test classes covering rule lifecycle/evaluator/execution/scope/migration
    update_type: 架构升级 + 新功能开发
    effect_scope: settlement_exception_rule_engine
  1.0.58:
    update_content: |
      Sprint41 Transport Scenario Validation & Operational Readiness
      - 8/8 场景目录覆盖（Business Scenario Catalog）
      - 4 个黄金数据集（最小自洽 base_data + scenario_s*）
      - 4 个自动化 BAT（标准化 6 项断言模板）
      - 8 份人工验证流程 + 结果记录
      - 不修改任何业务模型
    update_type: 质量硬化
    effect_scope: transport_scenario_validation
  1.0.59:
    update_content: |
      Sprint42 Debugging Pass — 清理 Sprint38 残留 + 修复 5 类代码缺陷
      - 清理 11 处 Sprint38 Rule Engine 残留引用（views/menus/ACL/tests/services/manifest）
      - 修复 security_settlement_groups.xml XML 结构损坏（孤儿行）
      - 修复 XML ID 引用错误 module_category_transport_management（缺 base. 前缀）
      - 重写 3 个 portal JS 文件适配 Odoo 18 ES module 系统
      - 补全 transport_request.py 缺失的 scene_id 字段
      - 沉淀 6 条教训至 context/history/decision_note.md
    update_type: Bug修复 + 质量硬化
    effect_scope: sprint38_cleanup + xml_validity + js_compatibility + model_field
  1.0.60:
    update_content: |
      Sprint48-A Cargo Node 层级模型升级（wd_tlms 1.0.111）
      - cargo line 升级为 Cargo Node：node_type / packaging_level / parent_cargo_line_id 树
      - 层级约束 container → handling_unit → package → piece
      - 容器设备字段 + pallet_*/piece_* 每托/每件字段 + source 追溯
      - equivalent_pallets 公式与 request 表头按顶层节点汇总
      - 单元测试 test_transport_cargo_node 6 项通过
    update_type: 架构升级 + 模型重构
    effect_scope: cargo_node_hierarchy + request_rollup
  1.0.61:
    update_content: |
      Sprint48-B Transport Document Projection（wd_tlms 1.0.112）
      - inquiry/quote Cargo Summary + cargo_source_reference 投影
      - order cargo_snapshot_version + snapshot_status，confirmed 后冻结
      - quote 接受自动建单并复制 cargo node 树快照 + 表头汇总
      - request 表头 rollup 与建单前一致性校验
      - 自动化测试 9 项通过（TestCargoNode 6 + TestDocumentProjection 3）
    update_type: 功能开发 + 单证投影
    effect_scope: inquiry_quote_summary + order_cargo_snapshot
  1.0.62:
    update_content: |
      Sprint48-C 测试库清空 + 业务场景回归
      - 清空 order/plan/quote/inquiry/request 及附属记录（search_count=0）
      - 新增 test_sprint48_business_scenarios：一柜20托 / 拆件双订单 / 快照冻结隔离
      - Sprint48-A/B/C 定向回归 12 项全部通过
    update_type: 数据清理 + 回归验证
    effect_scope: test_data_reset + business_scenario_regression
  1.0.63:
    update_content: |
      Sprint49 TLMS Business Rule Engine Foundation（wd_tlms 1.0.113）
      - Request 六维矩阵字段 + matrix_code/matrix_version + snapshot 冻结
      - cargo.line cargo_category 与单一 Root 校验
      - tlmp.carrier.capability + res.partner.carrier_capability_ids
      - tlmp.business.rule 配置化规则 + business_matrix 规则包
      - Rule Result 三态 + violations[] 多违规收集
      - Order Matrix Snapshot 快照复制与冻结
      - TestBusinessMatrix 9 项通过
    update_type: 架构升级 + 规则引擎
    effect_scope: business_matrix_rule_engine
  1.0.64:
    update_content: |
      Sprint49 Configuration 菜单补充（wd_tlms 1.0.114）
      - Configuration 新增 Carrier Capabilities 与 Business Matrix Rules 菜单
      - 列表/表单视图与动作
    update_type: 配置界面完善
    effect_scope: business_matrix_config_menus
