# Handoff — 2026-08-03

> 给新对话的交接文档。新会话请先读本文件，并严格按 `task-completion-discipline` 与 `odo-validate-loop` 执行。

## 1. 项目与 Git 状态

- 项目路径：`/Users/lijianqiang/Documents/odoo18_tms`
- 分支：`main`
- 最新提交：`116a47d44`（Ignore docs/codeexchange.xml）
- 工作区：干净（`docs/codeexchange.xml` 已从索引移除并加入 .gitignore，文件保留在磁盘）
- 最近推送：`2e697dfbe`（fee line source_type 修复）、`81db20c6c`（移除误提交）、`116a47d44`（gitignore）

## 2. 环境

- 数据库：`odoo18e_tms` @ `127.0.0.1:5555`（odoo / odoo）
- Odoo HTTP：`8089`（当前已释放，无监听）
- 日志：`debug_logs/odoo_181.log`
- 模块版本：`wd_tlms 1.0.101`
- XML-RPC 升级验证：登录 db `odoo18e_tms`，账号 `admin`，密码依次尝试 `123@Atmu` / `admin` / 空；调用 `ir.module.module.button_immediate_upgrade`
- Odoo shell：`echo "<python>" | venv/bin/python odoo-bin shell -c odoo.conf -d odoo18e_tms --no-http`；写操作必须 `env.cr.commit()`

## 3. 本次会话已完成

| 项 | 结果 |
|---|---|
| Scene 1 | 1.1 / 1.2 / 1.4 PASS；1.3、1.5 已修复待 UI 复验 |
| Scene 2 | 2.1 PASS；2.2 PASS；2.3 PASS；2.4 已修复待 UI 复验；2.5、2.6 未验证 |
| 3PL 流程 | 改为 Select Carrier → Create Customer Quote → Customer Accept Quote → Order |
| Quote 定价 | Customer Price = Carrier Cost + Margin；margin_rate = margin / customer price（小数存储，percentage widget 显示） |
| Quote Cargo Lines | 从 Request 柜明细复制，描述含柜号/BL；Fee Lines 可编辑 |
| Fee Line | `source_type` 默认 `commercial`（1.0.101） |
| Charge Item | 预置 6 条运输费用档案（1.0.100） |
| 技术债 | AD-009 ~ AD-014 已记录 |
| Skill | `odo-validate-loop`、`task-completion-discipline` 已安装到 `~/.codex/skills`，并在项目 `docs/context/skills/` 留档 |

## 4. Scene 2 下一步（新对话继续）

1. **Step 2.4 复验 quote 535**：Margin Rate=20.83%，Customer Price=480，Cargo Lines 有 `ewew - eewdd232323 - dfadfd432223`，新增 Fee Line 能保存（Fee Type 选 Transportation Fee 等）
2. **Step 2.5**：Quote Send to Customer → Accept Quote → 自动创建 Transport Order，检查 `order.carrier_id=Inquiry 承运商`、`order.partner_id=Quote 客户`、`scene_id=terminal_to_customer`
3. **Step 2.6**：Order Fees 页检查 `customer_charge`（应收）与 `carrier_cost`（应付）

## 5. 硬规则（新会话必须遵守）

- 禁止修改 `odoo/` 官方代码；第一优先查 `wd_tlms` 自定义代码
- 禁止只用 `-u wd_tlms --stop-after-init` 验证；必须 XML-RPC `button_immediate_upgrade`（等价 UI 点升级）
- Odoo shell 写操作必须 `env.cr.commit()`，并在新会话 `search_count` 复核
- 说“现在执行”后，下一条必须是工具调用；回合结束前六项自查：改完、验证、记录、提交、端口/服务、报告
- `git add -A` 前检查是否夹带无关文件（本次曾误提交 `docs/codeexchange.xml`）
- 8089 用完必须释放并确认

## 6. 参考文件

- `docs/context/history/decision_note.md`（教训 9~16）
- `docs/context/history/architecture_debt.md`（AD-001 ~ AD-014）
- `docs/business_validation/common_pre_check.md`
- `docs/business_validation/scene_1_terminal_warehouse.md`
- `docs/business_validation/scene_2_terminal_customer.md`
- `docs/business_validation/result_summary.md`
- `docs/context/skills/odo-validate-loop/SKILL.md`
- `docs/context/skills/task-completion-discipline/SKILL.md`
